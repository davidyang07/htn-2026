; The bridge tests run with the BACKEND venv (httpx + pytest only) -- they
; deliberately avoid importing the WorkSwarm engine, so this half of the
; integration is verifiable in CI without installing WorkSwarm.
;
;   backend/.venv/Scripts/python -m pytest workswarm
[pytest]
testpaths = tests
addopts = -q
